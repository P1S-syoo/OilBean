# 한강 오염 탐사 — 수상 3D 항해 페이즈 구현 계획서

- **엔진**: Unity 6000.3.11f1 — URP 17.3 · Input System 1.19 · Timeline 1.8.11 · DOTween Pro(보유) · URP Volumetric Fog(보유)
- **타깃**: PC (Windows) · 키보드(WASD) + 마우스
- **레퍼런스 이미지**: `C:\Users\seyo6\Downloads\Gemini_Generated_Image_7fvr6v7fvr6v7fvr.png` — 폐허 도시 사이 오염된 한강, 수면 위를 나아가는 잠수정 (부감 → 3인칭)
- **기획 의도**: 게임 시작을 풀 3D 수상 항해로 열어 세계관(오염된 서울)을 보여주고, 잠수정 덱 위에서 3인칭 캐릭터를 조작하다가 정화 목표 지점에서 잠수하면 기존 2.5D 횡스크롤 탐사 루프로 자연스럽게 이어지는 구조.
- **선행 완성물**: S0~S6 (2.5D 탐사 루프), World 무한 순환 맵 (2026-06-10)

> 이 문서가 이 페이즈의 단일 진실 공급원(SSOT)이다. 작업 완료 시 체크박스를 갱신하고,
> 세션이 바뀌어도 이 문서부터 읽고 이어서 작업한다.

## 목표 플레이 흐름 (전체 그림)

```
[게임 시작]
  → 인트로 컷신 (Timeline + Cinemachine: 폐허 도시 부감 → 강 위 잠수정 줌인)
  → Surface 상태: 잠수정이 강 스플라인을 따라 자동 항해
     · 플레이어는 덱 위에서 3인칭 캐릭터 조작 (구경/이동)
  → 정화 목표 지점 도달 → 잠수정 정지 + "잠수 가능" 안내
  → 해치 상호작용(E) → 잠수 연출: 카메라가 3인칭→사이드뷰(z-20)로 블렌드되며
     잠수정이 수면 아래로 가라앉음 (씬 전환 없음 — 끊김 0)
  → 기존 Dock/Dive 2.5D 루프 (탐사→연구/제작→정화→클리어)
  → (확장) 클리어 후 부상 연출 → 다음 목표 지점으로 항해 재개
```

## 핵심 아키텍처 결정

| 결정 | 선택 | 근거 |
|------|------|------|
| 상태 | `GameState.Surface` 1개 추가 | FSM 전환표 방식이라 추가 비용 낮음. 컷신은 별도 상태 대신 Surface 진입 시 Timeline 재생 + 입력 잠금(상태 폭발 방지, YAGNI) |
| 씬 전략 | **Main3D 단일 씬 — 수면(WaterY=24) 위에 수상 페이즈 통합** | "잠수 선택 → 사이드뷰 전환하며 물에 빠지는" 연속 연출은 씬 로드가 끼면 불가능. 기존 해수면·강변 빌딩 백드롭(Shore)·하늘을 그대로 재사용. (2026-06-10 씬 분리안에서 변경) |
| 잠수 전환 | 씬 로드 대신 **Cinemachine 블렌드 + 잠수정 하강** | 3인칭 카메라 → 사이드뷰(z-20) 위치로 블렌드하는 동안 잠수정이 수면 아래로 내려가 Dock 위치 도달 — 끊김 없는 한 컷 |
| 코디네이터 | FSM은 GameBootstrap이 단독 소유(`startOnSurface` 옵션), `SurfaceBootstrap`은 수상 리그 전담 + 잠수 인계 | 단일 씬에서 상태머신 2개가 생기면 진실이 갈라짐 — 기존 코디네이터 중심 유지 |
| 잠수정 이동 | **Unity Splines** 패키지 + 일정 속도 추종 | 곡선 강을 waypoint 직선 연결보다 자연스럽게 표현. 정화 목표 = 스플라인 위 거리 marker |
| 캐릭터 이동 | CharacterController + **잠수정 부모화**(덱 로컬 이동) | 움직이는 플랫폼 물리(Rigidbody 슬라이딩) 문제 원천 회피 — 잠수정 자식으로 두고 로컬 좌표만 이동 |
| 카메라 | **Cinemachine 3.x** — ThirdPersonFollow + Timeline 컷신 블렌드 | 인트로 부감→줌인→3인칭 인계를 카메라 블렌드로 무코드 처리 |
| 패턴 판단 | 신규 패턴 불필요 — 기존 코디네이터+FSM+이벤트 구조 재사용 | decision tree 0단계: 기존 구조로 충분 (YAGNI) |
| 환경 아트 | 그레이박스 우선 → VARCO 3D 교체 | 박스/평면으로 흐름 먼저 검증, 아트는 W7에서 일괄 교체 |

## 진행 요약

| ID | 시스템 | 우선순위 | 상태 | 진행도 |
|----|--------|----------|------|--------|
| W0 | 패키지·씬·상태 골격 | P0 (필수) | ✅ done | 5/5 |
| W1 | 강 스플라인 + 잠수정 자동 항해 | P0 (필수) | ✅ done | 5/5 |
| W2 | 3인칭 덱 위 캐릭터 조작 | P0 (필수) | ✅ done | 6/6 |
| W3 | Cinemachine 카메라 + 인트로 컷신 | P1 (중요) | 🟨 진행 | 4/5 |
| W4 | 수면·폐허 도시 그레이박스 환경 | P1 (중요) | ✅ done | 5/5 |
| W5 | 정화 스팟 도달 + 잠수 전환 | P0 (필수) | ✅ done | 6/6 |
| W6 | 복귀 루프 (클리어 → 수면 → 다음 구역) | P2 (선택) | ⬜ todo | 0/4 |
| W7 | 아트 교체 (VARCO 3D / 캐릭터) | P3 (확장) | 🟨 진행 | 10/11 |

**전체 진행도: 41/47 (87%)**

## 권장 구현 순서

W0 → W1 → W2 → W5(흐름 연결 먼저) → W3 → W4 → W6 → W7
— 핵심 루프(항해→잠수→2.5D 연결)를 먼저 끝까지 뚫고, 연출(W3/W4)은 그 다음.

---

## 시스템별 상세

### ⬜ W0 · 패키지·씬·상태 골격

**목표**: Cinemachine/Splines 설치, Surface3D 씬 생성, FSM에 Surface 상태를 추가해 뼈대를 만든다

`P0 (필수)` · `진행 0/5`

**구현 접근**: manifest.json에 패키지 추가 → 컴파일 확인. GameState에 `Surface` 추가하고 전환표에 `Surface→Dock`(잠수), `Clear→Surface`(복귀, W6 대비)를 등록. **단일 씬**: GameBootstrap에 `startOnSurface` 옵션을 추가해 같은 fsm이 Surface부터 시작, SurfaceBootstrap은 수상 리그 전담 코디네이터로 Main3D에 배치.

**작업 체크리스트**
- [x] `com.unity.cinemachine` 3.1.4 + `com.unity.splines` manifest 추가 → 컴파일 에러 0 확인
- [x] `GameState.Surface` 추가 + `GameFsm` 전환표 갱신 (Surface→Dock, Clear→Surface) + 시작 상태 생성자
- [x] `GameBootstrap.startOnSurface` 옵션 + `EnterDockFromSurface()` 잠수 인계 메서드
- [x] `SurfaceBootstrap.cs` 골격 — 수상 리그 코디네이터, RequestDive→GameBootstrap 인계
- [x] GameFsmTests에 Surface 전환 케이스 추가 (EditMode 3건)

**완료 기준 (Acceptance)**
- [ ] startOnSurface 체크 시 Surface 상태로 시작하고 에러 0
- [ ] RequestDive 호출로 Dock 인계 → 기존 탐사 루프가 정상 시작됨
- [ ] EditMode 테스트(Surface 전환 허용/차단) 통과

**리스크 / 주의**
- ⚠️ Cinemachine 3.x는 2.x와 API가 다름(`CinemachineCamera` 등) — 웹 레퍼런스가 2.x 기준인 경우 많아 공식 3.x 문서 기준으로 작성
- ⚠️ Surface 시작 시 autoStartDive는 잠수 인계 후로 보류(EnterDockFromSurface에서 처리) — 진입 경로 꼬임 방지

### ⬜ W1 · 강 스플라인 + 잠수정 자동 항해

**목표**: 잠수정이 강을 따라 흐르는 스플라인 경로를 일정 속도로 자동 추종한다

`P0 (필수)` · `선행: W0` · `진행 0/5`

**구현 접근**: SplineContainer로 강 중심선 작성(Main3D 해수면 위). `SubNavigator.cs`가 spline 거리 파라미터를 시간으로 누적해 위치·회전(접선 방향) 적용. 정화 목표 = 스플라인 정규화 거리(0~1) 배열로 정의, 도달 시 감속 정지 + 이벤트 발행(`OnArrived`). 씬 배치는 `Tools/한강/수상 리그 생성` 에디터 메뉴로 코드 재생성 가능(SurfaceRigBuilder).

**작업 체크리스트**
- [x] Main3D 해수면 위(WaterY+0.5)에 강 중심선 SplineContainer 작성 (완만한 S자 6노트, x -20~160)
- [x] `SubNavigator.cs` — 스플라인 추종(속도/감속·최저속도), 접선 수평 Slerp 회전, K&R+한글 주석
- [x] 정화 목표 지점 데이터(정규화 거리 배열 {0.5, 1.0}) + 도달 판정 + `OnArrived` 이벤트(예외 격리)
- [x] 도달 시 감속 정지 → 재출발 API(`Resume()`) (W6 대비)
- [x] 잠수정 그레이박스(선체+타워 박스) 배치 + FloatBob 부유 모션(모델 차일드 전용)

**완료 기준 (Acceptance)**
- [ ] 재생 시 잠수정이 강을 따라 자동 항해하고 굽이에서 자연스럽게 회전
- [ ] 목표 지점에서 감속 정지하며 OnArrived가 1회 발행됨

**리스크 / 주의**
- ⚠️ 스플라인 접선 회전이 급커브에서 홱 돌 수 있음 — 회전은 Slerp로 부드럽게
- ⚠️ 부유 모션(상하 진동)을 잠수정 루트에 걸면 자식 캐릭터가 같이 떨림 — 모델 차일드에만 적용

### ⬜ W2 · 3인칭 덱 위 캐릭터 조작

**목표**: 움직이는 잠수정 덱 위에서 3인칭 캐릭터를 WASD로 조작한다

`P0 (필수)` · `선행: W1` · `진행 0/6`

**구현 접근**: 캐릭터를 잠수정의 **자식**으로 두고 CharacterController 대신 로컬 좌표 직접 이동(움직이는 플랫폼 물리 회피). 덱 경계는 로컬 XZ 클램프(난간). 카메라 기준 이동 방향 변환(카메라 forward 투영). 애니메이션은 W7 전까지 캡슐+방향 화살표.

**사용 기술 스택**
- `Input System` — 신규 액션맵 `Surface` (Move=Vector2, Interact=E, Look=마우스)
- `로컬 이동 + 경계 클램프` — Rigidbody 미사용(플랫폼 미끄러짐 원천 차단)

**작업 체크리스트**
- [x] 입력 — 코드 생성 InputAction(DeckMove WASD 컴포지트, OrbitLook 마우스 델타)로 구현 (기존 returnInput 패턴과 일관)
- [x] `DeckCharacter.cs` — 카메라 기준 WASD 로컬 이동 + 이동 방향 회전, Step() 분리로 테스트 주입 가능
- [x] 덱 경계 로컬 클램프(±1.8/±4.6, 확대된 선체 4.5×1.5×11과 일치) — 물에 안 떨어짐
- [x] 잠수정 자식 부모화 — 이동·회전 중 로컬 위치 불변 PlayMode 테스트로 검증
- [x] 캐릭터 그레이박스(캡슐 + 전방 코) 배치 — SurfaceRigBuilder로 재생성 가능
- [x] 상호작용 프롬프트 훅 — 도달 시 Toast "정화 지점 도착 — E키로 잠수" + E 입력(W5에서 구현)

**완료 기준 (Acceptance)**
- [ ] 항해 중인 덱 위에서 캐릭터가 미끄러짐 없이 WASD 이동
- [ ] 덱 가장자리에서 더 못 나가고(클램프), 카메라 방향 기준으로 이동이 직관적

**리스크 / 주의**
- ⚠️ 부모 스케일이 1이 아니면 로컬 이동 거리가 왜곡 — 잠수정 루트 스케일 1 유지
- ⚠️ 마우스 Look과 UI 조작 충돌 — Surface 상태에서만 커서 잠금, UI 열면 해제

### ⬜ W3 · Cinemachine 카메라 + 인트로 컷신

**목표**: 인트로(도시 부감→잠수정 줌인) 컷신 후 3인칭 카메라로 자연스럽게 인계한다

`P1 (중요)` · `선행: W2` · `진행 0/5`

**구현 접근**: CinemachineCamera 2대 — ① 컷신용(Timeline 트랙으로 돌리 이동) ② 3인칭(ThirdPersonFollow, 캐릭터 타깃). Timeline 종료 시 우선순위 전환으로 Cinemachine 블렌드가 자동 처리. 컷신 중 입력 잠금 + 스킵(Esc/Space).

**작업 체크리스트**
- [x] 3인칭 CinemachineCamera — OrbitalFollow(마우스 궤도, 수직 -20~65° 클램프) + RotationComposer + OrbitCameraDriver로 W2에서 선구현

- [x] 인트로 부감→잠수정 줌인 돌리 — IntroDirector 코드 돌리로 구현(Timeline 대신 — 그레이박스 단계, 아트 확정 후 Timeline 전환 검토)
- [x] 컷신 중 입력 잠금 + 스킵(Space/Esc) → 즉시 인계
- [x] 컷신 종료 → 궤도 카메라로 Cinemachine 블렌드 인계 확인 (PlayMode 테스트 2건)
- [ ] (선택) 수면 가까이에서 카메라 살짝 흔들림(배 위 느낌)

**완료 기준 (Acceptance)**
- [ ] 게임 시작 시 이미지 구도의 부감 컷신이 재생되고 스킵 가능
- [ ] 컷신 끝나면 끊김 없이 3인칭으로 블렌드되어 즉시 조작 가능

**리스크 / 주의**
- ⚠️ Cinemachine 3.x의 Timeline 통합 트랙 이름/컴포넌트가 2.x와 다름 — 공식 3.x 샘플 참고
- ⚠️ 컷신 길면 페이싱 저해 — 10초 내외 + 스킵 필수 (E5 내레이션과 추후 결합)

### ✅ W4 · 수면·폐허 도시 그레이박스 환경

**목표**: 이미지 분위기(오염된 강 + 양안 폐허 도시)를 그레이박스 수준으로 만든다

`P1 (중요)` · `선행: W0` · `진행 0/5`

**구현 접근**: 수면 = 대형 평면 + URP 반투명 머티리얼(어두운 탁한 물색, 스크롤 노멀). 도시 = 기존 Shore 백드롭 사상을 3D로 — 박스 빌딩 변주(높이 hash) + 무너진 경사 박스, 강변 옹벽. 분위기는 보유한 URP Volumetric Fog + 어두운 하늘 그라디언트로. 진짜 아트는 W7(VARCO).

**작업 체크리스트**
- [x] 수면 평면(400×100m) + 탁한 물 머티리얼 — 잠수정 흘수선 아래 (노멀 스크롤은 아트 단계)
- [x] 강 양안 스카이라인 — 그레이박스 대신 LandmarkPlacer(Tools/한강/명소 배치, 로드뷰→VARCO 실물 3D)로 대체 (병행 세션 작업과 통합)
- [x] 잿빛 안개(Exponential, 원경이 안개에 잠김) — 볼류메트릭 패키지는 아트 단계에 도입
- [x] 부유 쓰레기/잔해 36개 해시 산포(박스/실린더 변주)
- [x] 디렉셔널 라이트 흐린 톤 조정 + 머티리얼 4종 에셋화(4.Art/Materials/Surface)

#### 명소 배치 표준 (2026-06-11 확정 — LandmarkScale/LandmarkPlacer)

실물 명소(roadview-to-3d 스킬 GLB)를 강 스플라인 기준으로 배치하는 규칙:

- **스케일**: `LandmarkScale.GameHeight(실물m) = clamp(2.7 × 실물^0.5, 3, 45)` — 실물 간 위계 유지 + 멱함수 압축. 세빛섬 20m→12u, 63빌딩 249m→43u, 롯데타워 555m→45u(캡). 모델 피벗과 무관하게 렌더러 바운즈 기준 균일 스케일 + 바닥 스냅.
- **수평**: 스플라인 t + side(좌/우안) + bankOffset — `중심선 + 법선 × side × (강폭절반 18 + offset)`, 정면은 강을 향함(yawOffset으로 모델축 보정).
- **수직**: floatOnWater=수면 흘수 0.15 잠김 / 강변=수면 기준선(WaterY+0.45, 수면 평면과 일치).
- **임시블록 제거**: WorldGen Shore의 수면 위 빌딩 기둥 생성 삭제(옹벽·인도 유지) + 그레이박스 City 삭제 — 하늘 영역은 실물 명소가 채움.
- **현황**: 세빛섬(t=0.47 우안, 첫 정화 목표 옆) 배치 완료, EditMode 35/35(LandmarkScale 테스트 4건 포함) 통과. 새 명소는 LandmarkPlacer.Defs에 추가 후 메뉴 재실행.

**완료 기준 (Acceptance)**
- [ ] 부감 컷신에서 레퍼런스 이미지의 구도·톤이 그레이박스로도 읽힘
- [ ] 프레임 안정(빌딩 박스 수백 개 수준에서 60fps)

**리스크 / 주의**
- ⚠️ 환경 디테일에 빠지면 일정 폭발 — 그레이박스 원칙 엄수, 아트는 W7로
- ⚠️ 수면 셰이더 욕심내지 않기 — 노멀 스크롤 1장으로 시작

### ⬜ W5 · 정화 스팟 도달 + 잠수 전환

**목표**: 목표 지점 도달 → 해치 상호작용 → 잠수 연출 → 기존 2.5D 루프로 씬 전환한다

`P0 (필수)` · `선행: W1, W2` · `진행 0/6`

**구현 접근**: SubNavigator.OnArrived → SurfaceBootstrap이 "잠수 가능" 상태 토글 + HUD 안내. 해치 트리거 안에서 E 입력 시: 입력 잠금 → **연속 잠수 연출(씬 전환 없음)** — Cinemachine 3인칭 카메라에서 사이드뷰 카메라(z-20, CamFollow 위치)로 블렌드하면서 잠수정이 DOTween으로 수면 아래 Dock 위치까지 하강 → 덱 캐릭터/수상 리그 비활성 → `EnterDockFromSurface()`로 기존 루프 인계. 현재 스팟 인덱스를 RunData에 기록.

**작업 체크리스트**
- [x] `RunData.surfaceTargetIdx` 추가(잠수 시 다음 목표 기록) + ResetRun 갱신
- [x] OnArrived → diveReady + Toast 안내(해치 트리거는 전체 덱 E 입력으로 단순화 — 덱이 좁아 충분)
- [x] E 입력 → 잠수 시퀀스: 입력 잠금 → DiveCam 블렌드(3인칭→사이드뷰) + 잠수정 DOTween 하강 (try-catch+한글 로그)
- [x] 블렌드 완료 → CamFollow 복원 → EnterDockFromSurface() 인계 → 수상 리그 비활성
- [x] DiveCam 종료 위치 = sideTarget + (0,0,-20) — CamFollow zOffset과 일치시켜 끊김 제거
- [x] PlayMode 테스트: 도달→잠수 가능 토글, RequestDive→Dock 인계+목표 기록 (통합 2건)

**완료 기준 (Acceptance)**
- [ ] 항해→도달→E→카메라가 사이드뷰로 돌며 물에 가라앉는 한 컷 연출→2.5D Dock까지 끊김/에러 0
- [ ] 전환 후 기존 탐사 루프(S0~S6)가 그대로 동작

**리스크 / 주의**
- ⚠️ 블렌드 도중 두 카메라 시스템(Cinemachine vs CamFollow) 동시 작동 금지 — 하강 완료 시점에 단 한 번 인계
- ⚠️ RunData는 공유 SO — Surface 필드도 ResetRun에 반드시 포함(잔류값 버그 방지)
- ⚠️ 수상 리그가 비활성된 뒤 2.5D 블록 스트리밍 범위(radiusY)에 수면 위가 걸려도 무해한지 확인

### ⬜ W6 · 복귀 루프 (클리어 → 수면 → 다음 구역)

**목표**: 스테이지 클리어 후 수면으로 복귀해 다음 목표 지점으로 항해를 재개한다 (하류→상류 진행감)

`P2 (선택)` · `선행: W5` · `진행 0/4`

**작업 체크리스트**
- [ ] Clear 상태에서 "수면 복귀" 선택지 → 부상 연출(잠수정 상승 트윈 + 카메라 사이드뷰→3인칭 역블렌드)
- [ ] RunData 스팟 인덱스 +1 → SubNavigator가 해당 지점부터 다음 목표로 Resume
- [ ] 수상 리그 재활성(덱 캐릭터·3인칭 카메라)
- [ ] 마지막 목표 도달 시 엔딩 플레이스홀더(추후 컷신)

**선조치 (2026-06-12 리뷰 지적)**
- SubNavigator.Resume()에 enabled/diving 가드, FloatBob 재활성 시 y 표류 보정 (이전 리뷰)
- SurfaceBootstrap의 수상 숨김/원복(배경색·입자·2.5D 백드롭)이 현재 "수상→잠수" 단방향 — W6 역전환은 상태 진입/이탈 바인딩(예: GameBootstrap 상태 변경 이벤트 구독)으로 재구성하거나, 수면 복귀 진입점에서 Start()의 숨김 블록과 동등한 처리를 다시 수행해야 함

**완료 기준 (Acceptance)**
- [ ] 클리어→수면→다음 구역 항해→재잠수가 한 사이클로 동작

### 🟨 W7 · 아트 교체 (VARCO 3D / 캐릭터)

**목표**: 그레이박스를 실물 3D 모델과 캐릭터 애니메이션으로 교체한다

`P3 (확장)` · `선행: W4, unity-varco 스킬` · `진행 10/11`

**작업 체크리스트**
- [x] 잠수정 실물 모델 교체 — PolyPizza `submarine1.glb` (바운즈 긴 축→진행축 Z 정렬, 11u 스케일, 선체 최대 렌더러 윗면=덱 높이, 모델 없으면 그레이박스 폴백)
- [x] 잠수정 VARCO 아포칼립스 모델 교체 (2026-06-12) — inpaint(nano_banana, 전체 마스크 t2i)로 녹슨 개조 잠수정 소스 이미지 생성 → image-to-3d 10K faces → `VARCO3DImports/sub_apoc.glb`. SubModelPath 교체, 재도색(Retint)은 PolyPizza 폴백일 때만 적용(VARCO 텍스처 보존). 폴백 체인: VARCO→PolyPizza→그레이박스
- [x] 캐릭터 모델 + 애니메이션 적용 — `MainCharacter.fbx` + `PlayerAnim.controller`(Idle/Swim, Mixamo), DeckCharacter가 이동 입력을 Speed 파라미터로 구동(임계 0.1). 루트 +0.55 보정(수영 클립 힙 오프셋)
- [x] 명소 실물 3D 배치 — 세빛섬 GLB(`Tools/한강/명소 배치`, t=0.47 우안 수상, 실물 20m→12.1u, bankOffset +1=궤도 카메라 반경 8 침범 밖) ※ 리그 재생성 시 Landmarks/Skyline도 날아가므로 배치 메뉴 재실행 필요
- [x] 폐허 빌딩 VARCO 생성·배치 + 수면 머티리얼 고도화 — roadview-to-3d로 `ruin_block`(압구정 상가)·`ruin_slab`(잠실주공 판상) GLB 생성(10K faces), `Tools/한강/폐허 스카이라인 배치`(SkylinePlacer)가 양안 21동 해시 변주(높이±20%·기울임·침하, 폭 상한 24u, 세빛섬 주변 비움). 수면은 `Game/MurkyWaterFlow` 셰이더(절차 노이즈 2겹 스크롤+마루 거품+스페큘러, DepthOnly 패스), 600×300m·y=WaterY+0.58(천장 블록 윗면 24.5를 덮음). 수상 동안 카메라 배경=안개색·수중 입자 OFF(SurfaceBootstrap, 잠수 시 원복)
- [x] 컷신 카메라 워크 아트 기준 재조정 — 시작 오프셋 (-26,16,-8): 후방 상공에서 하류(스카이라인·세빛섬)를 향해 열린 와이드샷, 잠수정은 에디터에서도 스플라인 시작점에 접선 방향 정박
- [x] 한강대교 스팬 — 교상 로드뷰 캡처→VARCO `bridge_arch.glb`(10K). 수상 동안 2.5D 백드롭(ChunkStreamer 블록·Sky 퀀드)이 다리처럼 보여 숨김(잠수 인계 시 복원), 스카이라인 침하 변주 제거(부서진 다리 오독 원인)
- [x] 스카이라인 침수 연속감 (2026-06-12) — +z 강변(사이드뷰에 보이는 쪽) 빌딩 하부를 수면 아래 5~9u까지 잠금(키 보정으로 수면 위 실루엣 불변) → 수중에서 빌딩이 끊기지 않고 이어짐. 수면 아래 전용 반투명 헤이즈 막(`Game/UnderwaterHaze`, z16, 플레이어 X 추적)으로 잠긴 부분 물 톤 처리. ⚠️ -z 강변(카메라 쪽)은 잠긴 하부가 2.5D 카메라를 가려 침수 제외
- [x] 스카이라인 무한 스트리밍 v3 (2026-06-12) — 배치 로직을 런타임 `SkylineStreamer`(Game.Surface)로 단독 이전: 메인 카메라 X 추적 셀(11u) 스트리밍이라 **수상·수중 양 페이즈 무한**. 프레임당 1셀 채움 큐(히치 방지·잠수 텔레포트 폭주 흡수), 다리는 터널 섹션 중심→소유 셀 정수 규칙(중복/누락 없음), radius 130u(안개 가시거리). 빌딩 변주 3종(+유리 오피스 `ruin_office.glb`), 간격 11u·틈 14/11%. 궤도 카메라 수직 하한 +3°(수면 아래 노출 방지). 에디터 메뉴(폐허 스카이라인 배치)는 스트리머 배선+정적 프리뷰만 — 플레이 시 Awake가 프리뷰를 지우고 스트리밍 인계
- [x] 스카이라인 구성 규칙 v2 (2026-06-12) — `Tools/한강/폐허 스카이라인 배치`(SkylinePlacer)가 단일 소유: ① 빌딩 근경 줄(강변 23~29u, 14u 간격, 20% 틈) + 원경 뒷줄(41~50u, ×1.25 확대, 14% 틈 — 틈 사이로도 먼 빌딩) ② 스플라인 양 끝 너머 ±90u 연장(시작 뒤쪽 허전함 제거, 안개 가시거리 커버) ③ **2.5D 터널 섹션(다리 밑 테마) 중심마다 다리 스팬**(수중 터널과 수상 다리가 같은 X에서 정렬, 밑면 수면+5.5u) — 현 범위에선 x=-90·+90 두 개. 명소(세빛섬 등)는 LandmarkPlacer 소유 유지
- [x] 2.5D 섹션/천장 제거 + 사이드뷰 통로 보호 (2026-06-12) — ① WorldGen 섹션 시스템(Open/Tunnel/Corridor)·천장 블록·터널 돌출 전부 삭제: 수중은 바닥 지형만, 해수면까지 전부 개방 ② 다리 스팬은 섹션 의존을 끊고 고정 간격(x=90+180k, 구 터널 중심과 동일 위치) ③ **빌딩 수중 가림 원인 수정**: EvalRiver의 스플라인 끝 너머 연장이 끝 접선의 z 기울기를 따라 표류 → 강변 빌딩(우안=카메라쪽)이 멀어질수록 사이드뷰 카메라(z-20)~플레이필드(z0) 통로를 침범. 끝 너머는 X축 직선 연장으로 변경 + 배치 후 통로 클램프(카메라쪽 max.z≤-22, 원경쪽 min.z≥+6, 다리 제외) 추가. 광역 검증(±300u, 렌더러 379개) 침범 0건, EditMode 37/37·PlayMode 28/28 통과
- [x] 분위기 통일 패스 — ① 잠수함 잿빛 군함 톤 재도색(원본 색 분류: 주황 프로펠러=녹슨 액센트, 시안 창=유지, 나머지=회청 SubHull/SubAccent 머티리얼) ② 잠수를 3D 잠수정 바로 아래 수중에서 시작(2.5D 잠수정을 nav.x로 텔레포트+관성 제거 — 시작 위치 순간이동 제거) ③ 잠수 후에도 수면 환경(수면·스카이라인·다리·명소)을 2.5D 백드롭으로 유지(3D 잠수정·수상 카메라만 끔) ④ WorldGen Shore 블록 레이어 제거(3D 스카이라인이 강변 백드롭 대체) ⑤ Sky 퀀드 잿빛(0.47,0.51,0.53)·z40(빌딩 뒤)
- [x] 구조물 크기 위계 + 모선 거점화 (2026-06-12) — ① 랜드마크 전용 배율 SizeMul=2(LandmarkPlacer, 세빛섬 12.1→24.1u) + 빌딩과 동일한 사이드뷰 통로 클램프(z -22~+6, SkylineStreamer 상수 공용) ② 잠수(E) 연출 변경: 모선(3D 잠수정)은 수면에 유지(가라앉음/비활성 제거), 2.5D 탐사정이 선체 바로 아래(WaterY-1.8)에서 descendDepth만큼 InQuad 하강하며 DiveCam이 추적 ③ dockPos를 하강 종착점으로 갱신(GameBootstrap.SetDockPoint) — R/강제 복귀 시 모선 바로 아래로 돌아와 수면 위 모선이 보임(복귀 거점). 덱 캐릭터는 잠수 시 숨김(W6 복귀 시 재활성 전제)
- [x] 수면·부유 생동감 패스 (2026-06-13) — ① 잠수정 흘수 보정(HullLift=extents.y×0.35 상향, 함교 포함 바운즈 중심 정렬이 선체를 깊게 잠그던 문제) + 덱 높이를 원통 선체 근사(바닥+지름)로 캡(단일 메시 VARCO 모델에서 HullTopY=함교 꼭대기 버그) ② MurkyWaterFlow에 정점 웨이브(월드 XZ 3겹 사인, _WaveHeight 0.28) — ForwardLit·DepthOnly 동일 변위, 수면을 150×40 분할 그리드 메시(WaterGrid.asset)로 교체(Unity Plane 10×10은 정점 부족) ③ FloatBob을 Update 사인 기반으로 개편(롤·위상 추가, 재활성 y 표류 해결) + 부유 잔해 36개에 해시 변주(진폭 0.06~0.14/주기 2.2~4.3/롤 4~12°/위상) 부착
- [x] 캐릭터 보행·입수 애니 + 잠수 연출 v2 (2026-06-13) — ① Mixamo Walking/RunToDive 임포트(`Tools/한강/캐릭터 애니 갱신`=CharacterAnimBuilder가 휴머노이드·루프·컨트롤러 배선, Walk 1.3배속) ② PlayerAnim.controller: OnDeck(bool)·Dive(trigger) 추가 — 덱은 Idle↔Walk, 물은 Idle↔Swim, AnyState→RunToDive→Swim ③ 잠수(E) 연출: 덱 캐릭터가 RunToDive 모션+전방 포물선(plungeTime 1.1s)으로 입수 → 2.5D 캐릭터가 아래를 보며(SetCinematicPose -90°) 자동 헤엄 하강(조작 불가) → 도착 시 자세 복원+조작 인계 ④ ClipHipOffset: 수영 클립(+0.85) vs 보행/입수(발 기준) 루트 차이를 상태별 보정 ⑤ 잠수정 15u 확대(SubLength), 캐릭터 스폰 선미 0.6(함교 회피), 보행 속도 2.0 ⑥ 수면 셰이더 Cull Off(수중에서 수면 보임), 수중 입자(Bubbles) 머티리얼 누락 수정(BubbleParticle.mat). 플레이모드 검증: 덱 스탠스/잠수 E2E/모선 수면 유지 스크린샷 확인, 에러 0. ⚠️ MCP 임시 캡처 카메라는 MurkyWaterFlow를 못 그림 — 검증은 `camera="Main Camera"` 직접 렌더로
- [x] 덱 보행 정밀화 + 구조물 점유 구간 (2026-06-13) — ① IdleStand.fbx: 덱 대기는 서있는 자세(IdleDeck 스테이트, Idle(물 트레딩)↔IdleDeck를 OnDeck로 전환, 구 Idle↔Walk 직결 제거) ② 보행 표면을 선체 MeshCollider + 하향 레이캐스트로 한정(DeckCharacter.TrySnapToDeck: 명중+법선 y≥0.65만 이동 허용, 매 프레임 발 스냅 — 부유 모션 추종, 떠있음/덱 이탈 해결) ③ 보행 속도 1.4m/s(실제 사람)+Walk 클립 원배속, PlayMode 실측 테스트 2건 추가(1초 이동거리·콜라이더 가장자리 정지) ④ **구조물 점유 구간**: LandmarkPlacer가 배치 실측 바운즈로 zone(x±반경+16, 강변 side)을 SkylineStreamer에 배선 — 빌딩(근경·원경)·다리가 zone을 비움(세빛섬 x=67±52: x=90 다리 제외됨), 다리 접안부 ±13도 빌딩 제외, 수상 부유 랜드마크는 사이드뷰 통로 클램프 제외(강 위 위치 유지). 하드코딩 SebitMin/Max 제거
- [x] 덱 보행 안정화 패스 (2026-06-13) — "걷다가 순간이동" 원인: Mixamo 클립이 in-place가 아니고 Animator.applyRootMotion 기본 true → 모델 차일드가 클립 루트 이동을 따라가다 루프마다 원점 복귀. 수정: ① 클립 임포트에 루트 XZ/Y/회전 Bake Into Pose(제자리 재생) ② applyRootMotion=false(이동은 DeckCharacter 전담) ③ 발 스냅 정밀화 — 단차 한계 maxStepUp 0.35(함교·배관 위로 점프 방지=장애물 차단), footSink 0.05(떠 보임 제거), snapLerp 6m/s(요철 y 부드러운 추종)
- [x] 덱 보행 안정화 v2 (2026-06-13) — ① **베이크 방향 정정**: XZ Bake Into Pose는 이동을 포즈에 남겨 루프마다 원복(순간이동)되는 원인 → XZ 베이크 OFF(루트모션 추출)+applyRootMotion=false(버림)=진짜 제자리 재생. Y·회전 베이크는 ON 유지 ② 기본 스테이트=IdleDeck(시작 직후 수영 모션 깜빡임 제거) ③ 스폰 높이를 빌더 선체 레이캐스트 실측으로(컷신 동안 접지) ④ IntroDirector가 enabled 대신 DeckCharacter.ControlLocked로 입력만 잠금 — 컷신 중에도 접지 스냅 유지(잠수 연출은 수동 이동이라 기존대로 enabled=false) ⑤ footSink 0.05→0.12
- [x] 덱 보행 영역 확장 (2026-06-13) — 잠수정 15u 확대 후에도 보행이 함교 주변 좁은 띠로 한정된 문제. 1차 클램프 deckHalf x 0.6→0.92·z 0.8→0.92배(선체 콜라이더 레이캐스트가 실제 한계가 되도록 넉넉히), walkableNormalY 0.65→0.5(둥근 선체 어깨까지 보행), maxStepUp 0.35→0.45. 임시 진단(덱 보행 영역 스캔)으로 측정: 보행 가능 영역 폭 4.9m × 길이 13.8m, 덱의 76%(막힌 24%=함교·급경사 측면). ⚠️ 에디터 모드 콜라이더 레이캐스트는 Physics.SyncTransforms() 선행 필요
- [x] 잠수정 폭 확대 + 캡슐 경사 보정 보행 (2026-06-13) — ① SubWidthMul=1.5로 가로 확대(함교 양옆 통로 확보, 앞↔뒤 이동 가능). ⚠️ 함정: 모델이 긴 축을 Z로 맞추려 Y90 회전돼 있어 로컬 X 스케일이 월드 Z(길이)에 적용됨 → 회전 여부로 분기해 월드 X 닿는 축(회전 시 로컬 Z)에 폭 배수 적용 ② DeckCharacter 보행을 캡슐 근사로 교체: ProbeColumn(중심+반경 4점 링)이 stepUp 위~stepDown 아래를 하향 레이캐스트해 경사·단차를 발 반경으로 타고 오름. maxStepUp 0.6/maxStepDown 0.7/bodyRadius 0.35/walkableNormalY 0.45. 측정: 보행 폭 4.9→7.4m(중앙 함교 양옆 각 ~2.5m 통로, 뱃머리 완전 연결)
- [x] 덱 보행 경사·단차 부드럽게 + 스폰 원복 (2026-06-13) — ① 스폰 x=0(중앙·선미) 원복(robust 레이가 평평한 면 보장하므로 안전) ② 보행을 중심 프로브 단독으로 단순화(ring 제거 — 턱 앞에서 미리 떠오르는 부작용·가장자리 초과 진행 제거): 발 밑 표면을 한 번에 잡고 snapLerp 16으로 부드럽게 보간 ③ maxStepUp 0.6→0.7(배관·돌출 턱은 넘되 함교 벽 차단), 이동속도 1.4→1.6 ④ 단차 등반 PlayMode 테스트 추가(0.4m 턱 올라서기 검증). ⚠️ 동기 Step 루프 테스트는 Physics.SyncTransforms()·enabled=false·cam 고정 필요(런타임은 무관)
- [x] 턱 범위 ↑ + 잠수 입수 방향 측면 고정 (2026-06-13) — ① maxStepUp 0.7→1.1(웬만한 턱·계단 통과) ② 입수 방향을 캐릭터 시선(deck.forward)이 아니라 **잠수정 측면(가까운 현=강물, nav.right×side)**으로 고정 + 그 방향 바라보게 회전 → 선체 쪽을 봐도 물로 뛰어 관통 방지, plungeDistance 3→6(넓어진 선체 확실히 넘김)
- [ ] 부유 잔해 프롭 실물 교체 — 현재 절차 박스/실린더 36개 유지(침수 빌딩 파편·표류물 모델로 교체 예정). 수면 위로 솟은 2.5D 산호 프롭 1개 위치 조정 필요

---

## 미해결 질문 / 결정 필요

- [x] **캐릭터 모델/애니메이션 소스**: Mixamo로 확정 — Swimming/SwimmingToEdge/TreadingWater 임포트, PlayerAnim.controller로 통합(2.5D·수상 공용)
- [ ] **Surface에서 연구/제작 접근 여부**: 덱 위에서도 연구/제작 UI를 열 수 있게 할지, 잠수 후 Dock에서만 할지 (기획서의 "정화선 거점" 개념과 잠수정의 관계 정리 — 현 계획은 Dock에서만)
- [ ] **덱에서 떨어질 수 있는지**: 프로토타입은 경계 클램프로 못 떨어지게 — 추후 떨어지면 리스폰+페널티 검토
- [ ] **인트로 컷신과 E5(Yarn Spinner 내레이션) 결합 시점** — W3는 카메라만, 내레이션은 E5에서
- [ ] 기존 `Main.unity`(구 2D 씬) 정리 여부

## 검증 규약 (기존 프로젝트 규약 승계)

- 각 W 완료 시: Unity MCP `read_console`(에러 0) → `run_tests`(EditMode/PlayMode) → 흐름 수동 확인
- 코드 컨벤션: code-rules 스킬 (K&R · 한글 한 줄 주석 · try-catch+한글 로그 · 요청 범위만 수정)
- 작업 후 `codegraph sync`
- 체크박스 갱신은 이 문서를 직접 수정
